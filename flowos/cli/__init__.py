"""
cli — FlowOS Command-Line Interface package.

This package provides the ``flowos`` CLI, the primary interface for human and
AI agents to interact with the FlowOS platform.

Commands:
    flowos workspace init     — Initialise a new agent workspace
    flowos workspace status   — Show workspace status
    flowos workspace checkpoint — Create a checkpoint
    flowos workspace revert   — Revert to a checkpoint
    flowos workspace log      — Show checkpoint history
    flowos workspace branch   — Create a task branch
    flowos workspace handoff  — Prepare a handoff
    flowos workspace archive  — Archive the workspace
"""
